sudo: required

services:
- docker

language: java
jdk: oraclejdk11

env:
  global:
  - IMAGE_NAME="$DOCKER_GCP_LOCATION/census-rm-case-processor"

before_install:
  - mvn fmt:check
  - docker login -u "${DOCKER_GCP_USERNAME}" -p "${DOCKER_GCP_PASSWORD}" "${DOCKER_GCP_REGISTRY}";

script: travis_wait mvn verify jacoco:report

after_success:
- if [ "$TRAVIS_BRANCH" == "master" ] && [ "$TRAVIS_PULL_REQUEST" == "false" ]; then
  docker login -u "${DOCKER_GCP_USERNAME}" -p "${DOCKER_GCP_PASSWORD}" "${DOCKER_GCP_REGISTRY}";
  docker push "$IMAGE_NAME";
  fi
- if [ "$TRAVIS_BRANCH" == "stamp_created_date" ] ; then
  docker login -u "${DOCKER_GCP_USERNAME}" -p "${DOCKER_GCP_PASSWORD}" "${DOCKER_GCP_REGISTRY}";
  docker tag "eu.gcr.io/census-rm-ci/rm/census-rm-case-processor" "eu.gcr.io/census-rm-ci/rm/census-rm-case-processor:ade_test";
  docker push "eu.gcr.io/census-rm-ci/rm/census-rm-case-processor:ade_test";
  fi
- bash <(curl -s https://codecov.io/bash)

cache:
  directories:
  - $HOME/.m2

branches:
  only:
  - master
  - stamp_created_date
